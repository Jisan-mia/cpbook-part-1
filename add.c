#include <stdio.h>
#include <conio.h>
main(){
    int x,y, sub;
    printf("Enter the value of x: ");
    scanf("%d", &x);
    printf("\nEnter the value of y: ");
    scanf("%d", &y);
    sub = x+y;
    printf("summition of two number is = %d", sub);
    getch();
}
